# ERRATA — 2026-09-25d (owner): main-churn disclosure, verification standard, resume order

1. DISCLOSURE. All main commits 2026-09-25 20:12–20:33Z (HALT fix, errata-25c,
   registry nonce_hash updates, ERRATA-25d) are OWNER ACTS. No force-push
   occurred; history is linear. MAIN-MOVED alarms for these commits are
   answered herewith; no investigation is owed.
2. REGISTRY REWRITE — disclosed owner exception. fleet2/activations/
   REGISTRY.md was rewritten by the owner during deployment (against its
   own "append-only" header). The CURRENT text on main is authoritative;
   "append-only; never rewrite" applies FROM NOW ON. This file is itself
   the append correcting that episode.
3. VERIFICATION STANDARD (amends LAW §1A for the v0 UNHASHED posture):
   boot-record matching is NORMALIZED EQUALITY — compare record lines after
   trimming trailing whitespace and normalizing line endings; equality of
   the five record fields is the standard. Byte-identity of encoding is NOT
   required (web-editor drift is expected). Any other difference = mismatch.
4. RULESET. The branch ruleset targets the DEFAULT BRANCH ONLY. Lane
   (arena/*) pushes are and remain unaffected. It currently carries no
   rules; completion (require-PR on main) lands in 2.0.1.
5. ACTIVATIONS CONFIRMED IN FORCE: A-2026-09-25-001 (WORKER-2),
   -002 (ORCH-2), -003 (BOSS-2), under policy sha256 0fe20a6057ec9fa2.
6. REGISTRY FREEZE. No further registry edits by anyone, owner included,
   until a 2.0.1 policy change. Every edit re-triggers boot verification
   fleet-wide.
7. RESUME ORDER. All roles: re-ground on this errata, re-verify with the
   normalized standard, and RESUME your missions (WORKER-2: M5-R;
   ORCH-2: cut M5-R/M4 and gate; BOSS-2: track the manifest, classes 1–6).
Signed: owner (this commit is the signature).

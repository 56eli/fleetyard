<!-- GENERATED by fleet2check control-render from fleet/CONTROL.log; do not edit -->
# control state — ORCH-2

checked_utc: 2026-09-26T10:04:50Z
main_head: 7d033ab
registry_head: ORCH-2 a86115d2
record_seq: 60
decision: PASS
note: DEFECT #52 FIXED BEFORE IT COULD FIRE - the gate was armed to FAIL the run this lane had just asked the owner to authorize. Cycle L's decision request (fleet/ORDERS/DECISION-REQUEST-QUANTUM-B-2026-09-26.md) asks the owner whether quantum b may fire; section 12 still carried a row reading "no receipt already declares the V2 holdout SPENT (the quantum-b run has not happened)", expected 0, verdict PASS if not spent else FAIL. That expectation was correct only while the run was blocked: the instant the owner rules and the worker fires a properly attributed one-shot, the row would have reported a FAIL against exactly the artefact the fleet is waiting for - and a dangerous FAIL, because "a receipt declares the holdout spent" is also how a REAL breach reads, so it would have needed arguing rather than reading. The row is now a decision procedure over the receipts (quantum_b_spend_verdict): no receipt binding v2 -> PASS, the one-shot unfired, with n the count of artefacts read so the PASS is not vacuous (defect #42); exactly one fully attributed receipt -> PASS, requiring an AUTHORIZATION citation, a run utc exact to the second WITH ITS SOURCE, a frozen-threshold digest (v2.7: thresholds fixed before the run and provable after it) and the sealed split it consumed; exactly one receipt missing any of those -> FAIL NAMING THE MISSING FIELD, because a one-shot is spent once and its receipt is the only evidence that survives it; two or more -> FAIL, the one-shot was evaluated MORE THAN ONCE, which no argument about which receipt counts can repair. MUTATION-TESTED T37-T40, including the branch that has never existed in any artefact - the compliant receipt - because that is precisely the branch a fixed expectation gets wrong; --selftest is now 40/40. Re-run at BOTH heads after the amendment: f5e2cf5 336 rows / FAIL 5 unchanged (the spend row still PASSes as unfired, but now for a reason that survives the run) and 34db0b0 335 rows / FAIL 13 unchanged, the same thirteen cycle K published; both goldens refreshed and --selftest 40/40 against the new golden. RULE ADOPTED AND RECORDED IN THE CURSOR, worth more than the row: A ROW WHOSE EXPECTED VALUE ENCODES "THE WORK HAS NOT HAPPENED YET" IS A FALSE CHARGE WAITING FOR THE WORK TO LAND. Two instances in one cycle, from opposite sides - item 0g demanded byte-identity from a file criterion 20.14c had ordered repaired (defect #51), and section 12 expected the absence of an artefact another act had just requested authorization to create (defect #52). Such rows must be decisions over artefacts rather than constants, and must be mutation-tested on the branch nobody has seen run. Still open, unchanged: the worker's v2.e and v2.f, item 12 and item 13b; the owner's quantum-b decision; BOSS-2's signal silence since 02:26Z and WORKER-2's backward utc column, both reported not ruled.
checks_logged: 60
seq_monotonic: yes

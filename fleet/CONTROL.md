<!-- GENERATED by fleet2check control-render from fleet/CONTROL.log; do not edit -->
# control state — arena/01a0d9d0-fleetyard

checked_utc: 2026-09-25T22:58:11Z
main_head: 7d033ab
registry_head: A-2026-09-25-002 a86115d2d5417f4a17be19e94e19f1e93e54e5e7d24c78e73a3c0f9c65e9435c14
record_seq: 27
decision: OK
note: CYCLE G continued: quantum-b PRE-REGISTRATION PROTOCOL written as a binding ANNEX of fleet/queue/pending/TASK-019.md and QUEUE PRIORITY published for WORKER-2. Rationale recorded in the annex: quantum b is the only path to a precision figure for C1-drop and C2-format and it is a ONE-SHOT run over a 33-transcript sealed holdout that cannot be replaced without a fresh salt, so everything that could be chosen AFTER seeing the result is fixed BEFORE the run - denominator (33 primary with per-file disclosure of the four fixture-adjacent transcripts plus 29 as a pre-registered sensitivity), flank floor 5 with the band 71/57/33/22 as a REPORT not a choice, the eight C1-drop params at shipped values (122/113/311/48/173/8/118/110), the seven C2-format rules, the four v1 configs, detector shas a0236325 and ef9ff4f2 plus the 13 pins at bf97d85, corpus zip 3f36c520, book store c0892fcd, ledger d42136c6 (invariant), policy 0fe20a60, and the TOKEN RULE stated explicitly (apostrophes and hyphens inside tokens, em/en dashes as separators, transcript-sha256 basename keys) because token-rule ambiguity is a documented gate-instrument defect class in this campaign. One-shot mechanics: exactly one execution, the run publishes holdout_reads which the gate verifies EQUALS the eval set (no more, no less), a pre-registered abort rule (a crash before any holdout byte is read, proved by an empty read log, may be retried once; any crash after the first read consumes the holdout), no threshold/detector/token-rule/denominator change between the pre-registration commit and the run commit, and one_shot_discipline verified from the TOOL SOURCE as I did for m4_q4_supplement.py. Required outputs D1-D6 include per-signal citations sufficient for my own 938/938-style claim test to be reproducible on the holdout rows, and a VERIFIED seeded/in_sample separation (expected 0 seeded:true because holdout transcripts carry no fixtures - verified and published, not assumed; the false prose in TASK-018 item 0d is exactly the failure this guards against, and an ABSENT flag is as much a defect as a wrong one). New criteria v2.12 (pre-registration commit exists BEFORE the run commit, verified by commit order and exact sourced timestamps per 20.14), v2.13 (receipt declares the holdout spent and holdout_reads equals the eval set), v2.14 (nothing added to the eval directory after the run except the receipt and an errata, verified from path history), v2.15 (seeded/in_sample verified on holdout rows), v2.16 (one-shot discipline verified from source). Quotation rules after PASS: precision only with denominator + floor + strata + site count + notation disposition + the four-file taint disclosure; NO extrapolation to the corpus; NO rate per transcript unless exposure-normalized with the ratio stated; the C1-drop expectation may be compared (19.9 at v1 exposure) but the deficit cause stays UNESTABLISHED; every figure carries split v2, one-shot, in-sample-free and the 27% hyphen-sensitivity caveat where A1 claims are involved. QUEUE PRIORITY for WORKER-2 (who per BOSS-2 cycle 31 was idle-cycling at 4fc40c8 awaiting exactly these results, now pushed at ee4ea5e): (1) TASK-020 item 8a - re-pin all six supplement artefacts, cheapest item and it ALONE unblocks the q3 re-gate; (2) TASK-018 items 0d-0g - unblocks the q2 re-gate and the adjudication set's usability in M6; (3) TASK-019 items v2.a+v2.b - unblocks quantum b and therefore TASK-015, with the new annex binding; (4) TASK-020 11a/11b/12 + criterion 20.13 and TASK-017 item 17.a; (5) TASK-021 tuning-half threshold sensitivity, lowest urgency and it never touches the holdout. ORCH-2 adds no further work while those are open (ERRATA-25f: never overfill the queue). BOSS-2 read at 21d9a2b: zero CONCERNs, zero controls, stall watch not fired, no order addressed to ORCH-2, REDIRECT-008 acknowledged and completed. NEXT: monitor for WORKER-2 repairs on the priority order, keep heartbeat and CONTROL cadence inside the work loop, and hold the gate-side scratch worktree at 4fc40c8 for the re-gates that item 8a and items 0d-0g will trigger.
checks_logged: 27
seq_monotonic: yes

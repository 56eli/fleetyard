<!-- GENERATED by fleet2check control-render from fleet/CONTROL.log; do not edit -->
# control state — arena/01a0d9d0-fleetyard

checked_utc: 2026-09-25T23:15:45Z
main_head: 7d033ab
registry_head: A-2026-09-25-002 a86115d2d5417f4a17be19e94e19f1e93e54e5e7d24c78e73a3c0f9c65e9435c14
record_seq: 29
decision: OK
note: GATE INSTRUMENT COMMITTED: fleet/gate-tools/orch2_verify.py re-runs every row of the ORCH-2 VERIFICATION LEDGER mechanically and read-only over a gate worktree, printing expected + observed + verdict + n (the number of comparisons actually performed) per row. Output at WORKER-2 head 4fc40c8 committed as fleet/gate-tools/orch2_verify_output_4fc40c8.txt: 107 ROWS, PASS 97, FAIL 7, INFO 2, PROXY 1. Two rules are built in because ORCH-2 broke both of them once (ledger section 5): R1 - every set-level or interval check reports n and a zero produced by zero comparisons is reported as VACUOUS, never as PASS (the fixture-overlap row demonstrates it: n=0 interval comparisons were possible because no adjudicated row shares a transcript with any fixture, so the row is INFO/VACUOUS-by-data and points at the decisive membership test, 0 of 122 with n=122); R2 - every byte-exactness count reports the denominator over which the comparison was DEFINED, so the 120 rows carrying span_start and span_end_matched are counted as 120/120 and the two rows without them (D-027, D-117, span null by construction) are verified separately via char_offset + detector_span, never as null == null. THE INVARIANT THAT MAKES THIS USEFUL: the instrument's FAIL set is EXACTLY the open-item set, no more and no less - criterion 20.10 tool_commit lacks the generator -> TASK-020 item 8a; the worker's published v1 row 187.9/0.44 vs the census-derived 187.5548/0.4451 -> item 11a; the q4 format-leg config digest covering a subset (8e7e35a2 vs 805241dd) -> item 14 / criterion 20.16; PATTERNS 5e carrying '98' and 'up to 11' unannotated -> item 11b (two rows); PATTERNS 5b-bis carrying the fuzzy timestamp 21:5xZ -> item 12 / criterion 20.14; PATTERNS 5b-bis repeating the false seeded sentence -> TASK-018 item 0d. INFO rows: split v2's fixtures-adjacent binding (item v2.a owed under criterion v2.5) and the vacuous-by-data interval row. PROXY row: the manifest's book_refs 242, which is not re-derivable from the ledger by substring proxy (mine gives 240) and is therefore never reported as a mismatch. Item 13 produces NO FAIL because both derivations do reproduce once the exact construction is known - the instrument records them as PASS WITH THE NOTE that the manifest states neither the join (newline-joined sorted lines without trailing newlines gives 58274f46) nor the key convention (basename over the two files in fixtures/confirmed/), which is exactly what criterion 20.15 asks to be fixed. FOUR DEFECTS THE INSTRUMENT FOUND IN ITSELF ON ITS FIRST RUN, published as ledger section 7.1 rather than quietly fixed, TWO OF THEM FALSE PASSES which is the dangerous direction: (1) wrong artefact path - signals-v2tuning.json lives under runs/m4-q3-format/ not runs/m4-q2-dropword/, producing a false FAIL of ABSENT for a file that is present and unchanged at b25651e4; (2) SLOPPY SECTION SLICE producing a FALSE PASS - 5e was sliced as 'from the first occurrence of the label to end of file' so supersession wording from LATER sections satisfied 5e's check; fixed by slice_section() which cuts from the heading to the next heading of the same or higher level and reports the slice length so an empty slice is VACUOUS not PASS; (3) a meaningless word-count heuristic producing three false FAILs - comparing the count of supersed* mentions to the count of stale digest occurrences cannot work because a correction block quotes the stale digest itself; replaced by a document-level test (every stale line either sits inside a SUPERSESSION block or has one later in the same document, the current digest appears, and findings/PROVENANCE.json is named as the binding of record) with the distance REPORTED rather than required, since under append-only discipline an inline marker would rewrite history - measured distances: findings/README.md L88 to L107, M4-q5-A1-CLAIM-RECONCILIATION.md L51 to L82, WORKER-2-M5R-DELIVERY.md L80 to L154; (4) case-insensitive marker matching mis-classifying an original stale line as 'quoted by the correction' because a lower-case 'supersedes' from an older errata about a different subject sits three lines above it in WORKER-2-M5R-DELIVERY.md; fixed by requiring the uppercase SUPERSESSION token for block detection. STANDING RULE ADOPTED: a gate instrument is run against a head whose answer ORCH-2 already knows before its output is trusted - this one was, every row reproducing the figures derived by hand in ledger sections 1 to 5, and the four defects surfaced precisely as disagreements with results already known. CONSEQUENCE FOR THE FLEET: when WORKER-2 lands item 8a the first FAIL clears and the q3 RE-GATE becomes a single instrument run plus an invariance check; when TASK-018 items 0d to 0g land the last FAIL clears and the q2 re-gate follows the same way; and any NEW FAIL on a later head is by construction a new finding rather than a re-litigation of settled ground. Nothing else moves: no detector promotable, no precision, no rate, no M6 figure, 122 and 48 signals CANDIDATE/PROVISIONAL-UNGATED, the 57 CERTAIN-leg-d rows restricted, TASK-015 BLOCKED behind quantum b, M6-P owner-accepted and not re-certified. CONTROL seq 29. NEXT: hold cadence at 300s, watch WORKER-2 for item 8a first, and re-run the instrument on each new head.
checks_logged: 29
seq_monotonic: yes

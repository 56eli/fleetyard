<!-- GENERATED by fleet2check control-render from fleet/CONTROL.log; do not edit -->
# control state — arena/01a0d9d0-fleetyard

checked_utc: 2026-09-25T23:29:14Z
main_head: 7d033ab
registry_head: A-2026-09-25-002 a86115d2d5417f4a17be19e94e19f1e93e54e5e7d24c78e73a3c0f9c65e9435c14
record_seq: 31
decision: OK
note: INSTRUMENT v2 AND A SELF-AUDIT THAT FOUND ORCH-2 GUILTY OF ITS OWN CRITERION. fleet/gate-tools/orch2_verify.py gained the three sections the q2 and q3 re-gates need, so those re-gates are now also a single run plus an invariance check: section 6 read-scope and one-shot discipline, section 7 drop-word signal evidence, section 8 LAW section 8 manifest completeness, section 9 an optional --self-audit over any tree. At WORKER-2 head 4fc40c8: 172 ROWS, PASS 145, FAIL 14, INFO 11, PROXY 2, and the FAIL set STILL MAPS EXACTLY ONTO THE OPEN ITEMS (item 8a as four rows - the q4 supplement's tool_commit plus one generator row per supplement; item 11a; item 14 as three rows - the format-leg subset plus two missing config_digest_note; item 11b as two rows for PATTERNS 5e; item 12 as two timestamp rows; TASK-018 item 0d). THE HOLDOUT IS PROVABLY UNTOUCHED BY EVERY TUNING-SIDE RUN, which is the check that matters most in this campaign: the q2 supplement declares holdout_reads [] with holdout_enforced true and split_counts equal to split v2 (197/33/230); runs/m4-q2-dropword/signals.json has keys SET-EQUAL TO THE V1 TUNING 193 with intersection against the v1 holdout of ZERO (n=193); merge.signals_total 122 and merge.signals_sha256 both match the actual file; ALL SIX part digests match and every part manifest declares holdout_reads []; the source-inheritance filter arithmetic closes exactly (raw 122 minus source_inherited 1 = 121; 121 minus deferred_holdout 7 = 114, the seven deferred being the v2-holdout signals of TASK-019 item v2.b); the q3 v2 run declares transcripts_read_count 197 equal to split v2 tuning with holdout_reads []; and all three q4 provenance files declare transcripts_read SET-EQUAL TO THE 37 SPENT-HOLDOUT TRANSCRIPTS with intersection against tuning of ZERO (n=37 each), with holdout_consumed true at run level alongside thresholds_frozen_before_run and the v1_tool_shas block. SECTION 7: transcript spans re-read byte-exactly 122/122, book citations found in the book store 122/122, dropped_words present and contained in the suspected span 122/122; the drop-consistency row is reported as PROXY NOT FAIL because the mechanical rule encoded here (remove every occurrence of each dropped word then compare token lists) is NOT the procedure the q2.6 gate used to separate 3 repetition artifacts from 6 partial overlaps, and until that procedure is encoded the row may not serve as a criterion - the binding rows are the byte-exact ones. SECTION 8: all three supplements carry the six required keys, an exact-to-the-second run_utc, a book-store binding OR an explicit stated N/A (the q3 supplement does this correctly: 'N/A for this detector - C2-format never reads the book store (stated, not omitted)'), status language forbidding promotion and rate, a COMPLETE detector_pin_defect disclosure (original_pin + defect + attribution_bridge, the bridge naming my own byte-identical reproductions), and detector_sha256_at_head equal to the actual tool digest; the q2 README names all eight C1-drop thresholds and the q3 README states the correct answer for C2-format - NO NUMERIC THRESHOLDS, SEVEN SHAPE PREDICATES. TIMESTAMP CENSUS UNDER CRITERION 20.14, now mechanical: 26 FUZZY TIMESTAMPS ASSERTED across ten files of the worker tree (fleet/LOG.md 11, tools/PATTERNS.md 4 at 20:1xZ/21:2xZ/21:3xZ/21:5xZ, fleet/branches/WORKER-2-M5R-DELIVERY.md 4, findings/M4-q5-A1-CLAIM-RECONCILIATION.md 2, findings/README.md, WORKER-2-TASK-019a-DELIVERY.md, WORKER-2-TASK-020-DELIVERY.md, runs/m4-q4-holdout/README.md at 21:3xZ which is a site NOT previously catalogued, tools/INHERITED-V1-MANIFEST.json, fixtures/v2/dropword.json), plus 4 sites that QUOTE a fuzzy value in order to supersede it and are therefore not instances; exactly ONE own-time-field offender (tools/INHERITED-V1-MANIFEST.json materialised_utc = TASK-017 item 17.a); fixtures/v2/dropword.json's fuzzy generated_utc is COMPLIANT because the same object carries generated_utc_exact plus generated_utc_exact_source, which is the lane's own repair pattern and is reported as INFO rather than flagged; 38 exact-to-the-second timestamps present and 43 minute-precision sites elsewhere. ITEM 12 IS THEREFORE EXTENDED with a proportionate repair order: load-bearing timestamps (a run, generation, materialisation or appended correction that must be orderable against a commit - the three SUPERSESSION blocks, the PATTERNS correction notes, the q4 README item-10 correction, the inherited manifest) become exact to the second with their source, recoverable from the committer timestamps of d7fee6e, 4fc40c8 and b2e0761; narrative timestamps (LOG.md prose, delivery notes) may stay date-plus-minute PROVIDED THE COMMIT SHA IS CITED BESIDE THEM; and every quoted fuzzy value goes in backticks so an audit can tell citation from assertion. SECTION 9 SELF-AUDIT OF ORCH-2'S OWN LANE, run with the identical regexes: 52 files, 11 FUZZY INSTANCES ASSERTED (fleet/CONTROL.log 3, fleet/GATES.md 6, fleet/queue/pending/TASK-020.md 1, fleet/queue/status.md 1), 19 fuzzy values quoted as citations, 0 own-time offenders, 171 exact-to-second, 66 minute-precision. ORCH-2 IS AN INSTANCE OF ITS OWN CRITERION 20.14 and says so in the ledger: the historical records are not rewritten (append-only), and from this cycle ORCH-2 stamps its own headers exact to the second and backticks every fuzzy value it quotes. FIVE MORE DEFECTS THE INSTRUMENT FOUND IN ITSELF, published as ledger section 8.2 numbered 5 to 9, TWO OF THEM PRODUCING FALSE RESULTS IN THE DANGEROUS DIRECTION: (5) curly apostrophes had to be NORMALISED not merely included - adding U+2019 to the in-token class still failed 8 of 122 rows because the data uses the curly form while dropped_words uses the straight one, so tokens('that’s') never equalled "that's"; normalising before tokenising took the row from 114/122 to 122/122, reproducing in my own tool the known curly-vs-straight gate-instrument defect class; (6) a RUN-LEVEL field demanded at PART level - holdout_consumed is a property of the run, and requiring it in each part manifest produced two false FAILs; fixing it also meant digging into the parts' nested inputs block, which turned two UNVERIFIED read sets into two VERIFIED ones (37/37, intersection tuning 0); (7) every generator checked against every manifest, producing meaningless rows such as q2's tool_commit 'lacking' m4_q4_supplement.py - fixed by pairing each manifest with its own generator (q2/q3 -> m4_t20_supplement.py, q4 -> m4_q4_supplement.py); (8) A REGEX THAT MATCHED THE SUFFIX OF A CORRECT VALUE - the pattern for minute precision matched '38:04Z' inside '2026-09-25T20:38:04Z' and reported 19 FALSE OFFENDERS for own-time fields that are all exact to the second; fixed by fullmatch, with the rule recorded that a pattern classifying a whole value must match the whole value; (9) AN AUDIT THAT COULD NOT TELL ASSERTING FROM QUOTING - the first self-audit reported ORCH-2's lane as the worst offender in the fleet because gate records quote fuzzy timestamps in order to report them; fixed by classifying each hit as instance or citation from its immediate wrapping and the preceding 60 characters, and reporting both, with the rule that a defect census must exclude citations of the defect and say how it decided. Also recorded honestly: the instrument crashed once on a duplicated note= keyword argument while these rows were being added, a reminder that the instrument is code and owes the same review as any delivery. NOTHING ELSE MOVES: no detector promotable, no precision, no rate, no M6 figure; 122 and 48 signals stay CANDIDATE/PROVISIONAL-UNGATED; the 57 CERTAIN-leg-d rows stay restricted; TASK-015 M6-Final stays BLOCKED behind TASK-019 quantum b under the pre-registration protocol; M6-P owner-accepted and not re-certified. M4 scoreboard unchanged: q1 PASS, q2 FAIL, q3 FAIL on item 8a alone, q4 PASS, q5 PASS. CONTROL seq 31. NEXT: hold cadence, watch for WORKER-2 to take item 8a first (it alone unblocks the q3 re-gate, which is now one instrument run), and re-run the instrument on every new head.
checks_logged: 31
seq_monotonic: yes

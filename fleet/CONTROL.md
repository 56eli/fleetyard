<!-- GENERATED by fleet2check control-render from fleet/CONTROL.log; do not edit -->
# control state — ORCH-2

checked_utc: 2026-09-26T09:51:52Z
main_head: 7d033ab
registry_head: ORCH-2 a86115d2
record_seq: 57
decision: PASS
note: CYCLE-L DOCUMENTS PUT THROUGH THIS LANE'S OWN CENSUS - and three of them failed it, which is the point of running it. A cycle that charges the worker with two asserted fuzzy stamps while asserting fuzzy stamps of its own is the hypocrisy self-item O-4 exists to prevent; --self-audit reported 5 offenders in amendable docs, each a DIFFERENT kind of mistake, and each was fixed in the document rather than in the classifier. (1) fleet/ORCH-2-REPAIR-MAP.md matched on a PLACEHOLDER: entry 1 told the worker to "replace each asserted NN:NxZ", and FUZZY_TS's third alternative ([xX]Z at a word boundary) matched the literal token - the census cannot tell a description of a shape from an instance of it, so the map now describes the shape in words ("an x standing where a digit belongs"). The regex was deliberately NOT loosened: it feeds the published worker census, measured identically at two heads, and widening it mid-cycle would move a charge already filed. RULE: when documenting a pattern, do not print a token the pattern matches. (2) fleet/ORCH-STATE.md quoted correctly but too far from its keyword - "the outage approximation ~02:1xZ" is a quotation, but CITATION_CTX looks back 60 characters for fuzzy/imprecise/supersed/was/quoted/offender/defect and "approximation" is not among them; reworded to "the fuzzy outage stamp ~02:1xZ", because the honest fix is to MARK the quotation, not to widen a keyword list to suit one sentence. (3) fleet/CONTROL.md carried three of them and is NOT A DOCUMENT: fleet2check control-render generates it from CONTROL.log, so its text is the append-only log's text, and seq 56 quotes the worker's two stamps as evidence, which is correct in a record; CONTROL.md is therefore classified with the append-only record, on the stated principle that exempting a RENDERING of a record is not exempting a CLAIM. After the three fixes: asserted-fuzzy 28 (all inside append-only records), citations 36, amendable 0, and all three self-item rows PASS. The standard run is untouched - 336 rows, golden byte-identical - because the self-item row lives inside the --self-audit block; --selftest 36/36. Recorded in ledger §24.7.
checks_logged: 57
seq_monotonic: yes

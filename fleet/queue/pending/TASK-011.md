# TASK-011 — M5 exploratory full-corpus RAW signal census (not an audited-findings release)

milestone: M5 full-corpus sweep, **provisional first pass** under BOSS REDIRECT-007
status: OPEN — NEXT ACTIONABLE after ACTIVE PAUSE TASK-010 independently PASSes; supersedes earlier task-id order for deferred TASK-007/008/009
cut: 2026-09-25T00:10Z by successor orchestrator arena/01a0d5b7-fleetyard
worker lane: arena/01a0d581-fleetyard. Target before original worker-shift wall ~2026-09-25T02:23Z; worker's own pill/caps always outrank schedule.

## Mission and evidence boundary
The owner asks "how bad is it across the whole corpus?". `corpus/docdocgo/overlays/` from SHA-256 verified `docdocgo-fixes.zip` holds **230** UTF-8-decoded transcript `.txt` files plus `manifest.json` (231 directory entries, NOT 231 transcripts). Only `.txt` are suspect transcripts. The reliable book store is `corpus/docdocgo/html/merged-book-texts_json_1.js`, with 20 Hawkins books + 4 non-Hawkins entries; Book-of-Slides OCR in `extra-sources/` is UNDECIDED, exclude. The corpus is frozen and never committed or edited. Current usable modules: A1-repetition, A2-nonsense (after TASK-010 fixes a bilingual false HIGH), B1-contradiction and B2-misquote. **DO NOT run A4-confusion in this sweep**: its independent fixture recall is 0/16, its 0/59 clean FP estimate is VOID after tuning on those scored passages (owner ERRATA-2026-09-25 §3). Drop-word and speaker/format detectors do NOT exist; absence is not a zero error rate. B1/B2 `0/59` are in-sample checks, not held-out FP estimates; no detector precision has been measured.

## One focused deliverable
Run exactly A1, A2, B1 and B2, excluding A4, against every available `.txt` in sorted order. If the runner only supports `--family A|B|AB`, add a minimal, tested selector for these four (or a separate stdlib wrapper); do not change detector logic beyond the already-gated TASK-010 repair. Store reproducible **raw signal** records under an explicitly provisional path such as `runs/m5-raw/` (not as confirmed findings): per record transcript path, paragraph + char offset, exact quoted text, detector id(s), raw runner confidence, proposed text, book_ref if available, and review_status=`unreviewed`. Provide a master index/manifest: corpus SHA, worker/tool commit SHA, detector selection, run command, per-file processed counts and per-detector/combined totals, year breakdown (year unknown separate), zero-hit transcripts, failed reads, reviewed count vs pending count. A runner-labelled HIGH without human review is an unreviewed CANDIDATE-class raw signal for reporting; never count it in a headline HIGH error rate. Known **16** CERTAIN hand-read M1 fixtures are separate from raw detector records; dedupe before any future findings ledger. A transcript is "audited" ONLY if detectors ran *and* a finding pass reviewed its emitted records; otherwise report detector-run but pending-review, even if it has a hand-found fixture. Do not silently equate 230 processed with 230 audited.

## Acceptance (raw-coverage gate, NOT M5 certification)
1. If time permits, exactly 230/230 `.txt` processed, `manifest.json` excluded; if partial, commit and report exact processed/230, pending, audited/230, failures and next file at handoff (a partial honestly labelled result is acceptable as provisional). No writes to `corpus/**`, no network, Python stdlib only, outputs restricted to named runs/index/tests.
2. A1, A2, B1, B2 only; A4 excluded from run/results and explicitly documented. Regression test selector and smoke-check both a book-referenced B result and a repetition/language sample; independently re-read sampled output offsets and book refs against frozen corpus. Retain 16 CERTAIN fixture evidence in a separate reviewed index if useful, never auto-promote raw signals. All raw unreviewed runner labels (including nominal HIGH) appear ONLY in a separately labelled CANDIDATE/unreviewed signal section, never blended into any headline class rate.
3. `python3 -m unittest discover -s tests` green before every push, count >= previous 87, with deterministic run counts on same head; no test dropped. Report clean check 0/59 as **in-sample only** and precision as **unmeasured**; no claim of representative false-positive rate or full-corpus error density. Show coverage truth: transcripts detector-run vs finding-pass audited vs pending. Disclose the unmeasured drop-word and speaker/format classes.
4. This task may PASS its **raw-sweep delivery** once counts/citations are independently gated, but M5 milestone stays UN-CERTIFIED until detector precision has a human-reviewed sample, clean split is genuinely held out (REDIRECT-005 gate 5), and findings/ one-per-transcript plus master index has a reviewed evidence chain. Those requirements are not waived by the deadline.

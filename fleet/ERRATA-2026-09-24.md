# ERRATA — 2026-09-24 (owner)
1. PR transport: the owner may direct agents to open PRs as delivery
   vehicles; agents still never merge. Owner merges = integration, not a
   fleet merge (amends VISION §3 / CANON §10 accordingly).
2. corpus/ is intentionally NOT in git. Every role reproduces it locally
   from the sha-verified docdocgo-fixes.zip. If ever committed: git add -f
   (docdocgo/.gitignore hides txt-files/*.txt) + per-file sha256 manifest.
3. Bootstrap defect (appendix tails A-F truncated) caught by hub consult;
   fixed on the seed lane before merge.

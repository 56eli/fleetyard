# ERRATA — 2026-09-25g (owner): 24-HOUR SHIFT EXPECTATION — turn discipline (OVER-RIDING)

1. OWNER RULINGS, VERBATIM — over-rule all conflicting text:
   "The orchestrator handed off after just one or two hours. This is a
   failed state. Assume 24 hours expected."
   "The boss also halted. This is a design flaw, it could simply push
   documentation for me to read."

2. SHIFT EXPECTATION: a shift spans a full day (~24 h) of campaign work.
   Ending a turn after 1–2 h with a status essay is a FAILED STATE.

3. TURN DISCIPLINE:
   a. A turn ends ONLY for: platform necessity, a genuinely blocking
      FAIL, or shift HANDOFF. Never for status. Never for idleness.
      Never because the next cycle "could report later".
   b. Within a turn: work as many cycles as the platform allows.
   c. Before any turn ends (any reason): push all state + a cursor line
      so the next turn resumes EXACTLY where this one stopped. Turns are
      invisible plumbing; the shift is continuous across them.

4. STATUS = DOCUMENTS, NOT CHAT. Scoreboards, manifests, cycle
   summaries, trackers, alerts: pushed to the lane as files (LOG,
   heartbeats, STATE, ALERTS). The owner reads the repository. A chat
   status essay is never a reason to end a turn.

5. SPECIFIC CARRY-ONS:
   - ORCH-2: M5-R PASS acknowledged (13 criteria, fresh replay, own
     parsers — exemplary). TASK-014 q1 handling endorsed (proportionate,
     recorded deviation, no pause — correct). Fresh sealed split v2 is
     AUTHORIZED: cut it as a queue task (new salt, sealed before further
     tuning, evaluated once); M6-Final headline waits for it.
   - WORKER-2: the pause is ALREADY LIFTED (ORCH-2 PASS @ 45959ca) — no
     signal-waiting. Claim TASK-018 now (leg-(d) adjudication of the 122
     signals + 4 fixtures, items 0/0b first), then TASK-017.
   - BOSS-2: the tracker you produced is exactly right AS A PUSHED
     DOCUMENT — keep publishing it to your lane; never end a turn to
     present it.
   - STANDARDS.md leg-(d) fold-in lands by owner edit (see ERRATA-25e
     §2 for the text; recorded here so gating may cite 25e until then).

6. STALE-HEARTBEAT LESSON: ORCH-2's owned defect (cadence lines stale
   mid-review) is the correct fix pattern — control/heartbeat lines are
   written INSIDE long work loops. All roles: cadence beacons run
   through deep work, never pause for it.

Signed: owner (this commit is the signature).

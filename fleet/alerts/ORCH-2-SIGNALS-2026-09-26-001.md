# ALERT — FLEET SIGNALS — 2026-09-26T09:57:29Z — ORCH-2 (facts to BOSS-2 and owner; no verdict invented on signals)

Class: fleet-signal coherence. ORCH-2 gates evidence; **BOSS-2 owns signals**, so everything below is reported, not ruled.
Nothing here proposes a control against any lane.

## 1. BOSS-2's signals stop at 02:26Z

Read at BOSS-2 head `6e17567`: `fleet/heartbeats/BOSS.log` ends at **2026-09-26T02:26Z, cycle 73** — *"WORKER-2 @ f5e2cf5;
ORCH-2 @ 0937097; stall watch clean under ERRATA-25f/25g; zero concerns; cadence active."* Cycles 66–73 all report **zero
controls**, and every one of them witnesses ORCH-2 at `0937097`. That is ~7.5 h before this alert was written.

**Cause, bounded rather than guessed:** the GitHub credential died between 02:01:46Z and 02:17:07Z (bounds from CONTROL seq 46
and the first cycle-K document stamp; the exact second is not recoverable because the credential died between two calls), the owner restored it in the window between **09:11:31Z** (the mtime of `.git/HEAD` — this sandbox did not exist before the recreation) and **09:14:43Z** (the committer time of `af8444d`, the first push that succeeded after the outage) — two hard bounds, from git and the filesystem, instead of an estimate — and ORCH-2's sandbox was **recreated** at 09:11:31Z, destroying its git history (recovered — see §3). BOSS-2's silence begins inside that window and is most likely the same platform event. **No Class-2 charge is proposed
against BOSS-2:** ERRATA-25f makes liveness a test of signals rather than output, and here the fleet's signals were dark for a
platform reason, not a lane reason.

**Consequence the owner should see:** if BOSS-2 does not resume, the fleet has no boss lane reading cadence, and ORCH-2 will
not assume that authority — it will keep gating and keeping its own cadence, and will keep writing reports like this one where
BOSS-2 can read them when it returns.

## 2. ORCH-2's own signal gap, disclosed

`fleet/CONTROL.log` seq 54 (03:00:26Z) → seq 55 (2026-09-26T09:57:29Z) is a gap of about six hours forty-nine minutes, and
`fleet/heartbeats/ORCHESTRATOR.log` has the same gap. Cause: the credential outage plus the recreation, i.e. this lane could
neither publish nor, after the recreation, exist. Both entries say so in their own text rather than leaving the gap to be
inferred. The eight commits produced during the outage were local-only and are now published (`af8444d`, `8a0047e`).

## 3. What the recreation destroyed and what it did not

Destroyed: the git history carrying CONTROL seq 47–54 (eight commits; `cat-file` answers *"not a valid object name"* for
`f44d00e`, `f9ec6a8`, `74be23c`; the reflog held only the clone and the checkout). Not destroyed: the **files** — the platform
snapshot restored CONTROL.log at seq 54, the 331-row instrument with selftest 31/31, and every golden. Recovery: fetch the
lane, `git reset 0937097` (the true published head), one commit `af8444d` — 15 files, +1831/−90, **no deletions**, a strict
superset — pushed. No force-push, and no cycle re-written from memory while the files were on disk. The diagnosis came from a
push rejected **non-fast-forward against a remote that had not moved**, which is worth remembering: that rejection means the
local base moved, not the neighbour.

## 4. WORKER-2's CONTROL.log utc column — the cadence input BOSS-2 reads

Gated at `f5e2cf5`, 62 rows: **53 exact / 9 minute-precision**; no row stamped after the file's last commit; **seq values used
more than once: 10, 11, 12, 13, 14, 15, 38, 39, 45, 52**; and **new since cycle K — utc going BACKWARDS**:
`2026-09-26T01:26:40Z` followed by `2026-09-26T00:57:47Z`. Two consequences for a cadence reader, stated as facts: a
29-minute backward step places a later cycle earlier in any reconstruction of liveness from that column, and seq is **not** a
unique key in that log, so it must not be joined on. ORCH-2's own log is rendered by `fleet2check control-render`, which
enforces uniqueness and monotonicity (`record_seq 58 · seq_monotonic yes` after this cycle).

Payload: gate cycle L in `fleet/GATES.md`, ledger §23.15 and §24 in `fleet/ORCH-2-VERIFICATION-LEDGER.md`, the open-item map
bound to `f5e2cf5` in `fleet/ORCH-2-REPAIR-MAP.md`, and the owner decision request in
`fleet/ORDERS/DECISION-REQUEST-QUANTUM-B-2026-09-26.md`.

# ALERT ORCH-2-PLATFORM-2026-09-25-001 — GitHub credentials dead in the ORCH-2 sandbox: cannot push, cannot fetch

**Raised by:** ORCHESTRATOR, activation ORCH-2 (A-2026-09-25-002), lane `arena/01a0d9d0-fleetyard`.
**Raised at:** 2026-09-25T21:41:33Z. **Class:** platform necessity — not a lane dispute, not an integrity question about any
delivery.

## 1. What happened

At 2026-09-25T21:41:33Z every GitHub call from this sandbox began failing on credentials:

- `gh api user` → **HTTP 401 Bad credentials**; `gh auth status` → *"The github.com token in `GH_TOKEN` is no
  longer valid."*
- `git push origin arena/01a0d9d0-fleetyard` and `git ls-remote origin` → `fatal: could not read Username for
  'https://github.com': terminal prompts disabled`.
- No fallback credential exists on disk (`~/.git-credentials`, `~/.netrc`, `~/.config/gh/hosts.yml`,
  `.git/credentials` all absent; no `credential.helper` configured). The token is supplied by the environment.

Pushes earlier in this shift succeeded (`31ce8dd` at 21:24:22Z). The token expired or was revoked between then
and now.

## 2. Exact consequences for the fleet

1. **I cannot push.** The TASK-018 gate record — verdict **FAIL / INCOMPLETE**, CONTROL seq 19, plus items 0d–0f
   and criteria L7–L9 appended to `fleet/queue/pending/TASK-018.md` — is **committed locally as `8cd2f5c`** on
   `arena/01a0d9d0-fleetyard`. **Origin still shows `31ce8dd`.** The document exists and is complete; it is not
   yet where the fleet reads.
2. **I cannot fetch.** From this point my fleet visibility is blind: I cannot read new owner messages on `main`,
   BOSS-2 orders or CONCERNs, or further WORKER-2 deliveries. `main_head 7d033ab` and the registry digest
   `a86115d2…435c14` in my CONTROL lines are therefore the **last read** values, not current ones, and any
   authority check I perform offline is against `7d033ab` as of 21:24Z.
3. **My cadence will look stale on origin even though I am working.** Per ERRATA-25f liveness is SIGNALS
   (heartbeat + CONTROL.log at cadence) *as pushed*. Those pushes are blocked. **Message for BOSS-2: staleness
   after 21:24Z is this outage, not idleness, not a halt, not defiance.** A Class-2 staleness finding against
   ORCH-2 after this timestamp would be misdiagnosed.

## 3. What I am doing about it

- Continuing the gate queue **offline** against the already-materialized worktree `/home/user/gate-scratch/w-71c`
  (WORKER-2 head `ffb8811`, corpus and evidence in place): **TASK-019a → TASK-020 → TASK-017**. All of these can
  be verified from local bytes; none needs the network.
- Writing every record to disk and committing locally, so nothing is lost and the whole backlog pushes in one
  pass when credentials return. Local head after this alert: see `fleet/CONTROL.log`.
- **Not** inventing authority, **not** relaxing any criterion because the network is down, and **not** treating
  the outage as a reason to stop gating.

## 4. Owner action required

**Reconnect GitHub for this session in Arena.** Nothing else unblocks the push channel from inside the sandbox;
I cannot and will not ask for or handle tokens in chat.

On reconnect my first act is: `git fetch`, re-check the fleet (owner messages, BOSS-2 orders, WORKER-2 head),
push the backlog (TASK-018 gate + this alert + whatever offline gates completed), re-verify the registry digest
against the *current* `origin/main`, and resume the 300s cadence.

## 5. Nothing is being held back

No verdict is pending on the outage. The TASK-018 verdict is recorded in full at seq 19: **FAIL / INCOMPLETE, no
pause** — substance verifies (57/57 promoted rows byte-exact under my own strict reconstruction; replay
byte-identical; §8 manifest exemplary; ledger `d42136c6` untouched so the M5-R PASS stands; suite 217 OK
skipped=1), with four repairs owed (false `seeded` sentence; two duplicate promoted sites so 57 rows = 55 sites;
six `%`↔`percent` notation promotions where no *word* is absent; no stratification of the 57, of which 27 are
interjections/fillers) and one gate question **routed to the owner and BOSS-2**: does clause d-i reach
speaker-side fillers and notation variants? Not mine to decide.


---

## 6. Resolution and a second platform event (2026-09-25T22:33:26Z) — append-only

**Credentials restored by the owner** (reconnect in Arena). `git ls-remote`, `git fetch` and `git push` work again;
`gh api user` now returns **403 "Resource not accessible by integration"** — a token *scope* limit, not an auth failure,
and it does not affect git operations. Note for the record: no `gh`-API-only step may be relied on in this lane.

**Second event: the sandbox workspace was recreated while the outage was open.** On reconnect I found
`/home/user/fleetyard` freshly cloned at `origin/main 7d033ab` with my lane branch checked out at its **base**
commit `2ed0b9b`, and the six cycle-F commits **absent as objects** (`8cd2f5c`, `cba2550`, `3dd0b45`, `1ca3941`,
`430706b`, `4d297d0` → `git cat-file -t` = ABSENT; reflog holds only `clone` + `checkout`). `/home/user/gate-scratch`
(the gating worktree at `ffb8811`, with the materialized corpus) was **wiped entirely**.

**What survived and how the record was restored.** The working-tree documents survived via the workspace snapshot:
`fleet/GATES.md` (all four cycle-F gate records), this alert, `fleet/ORCH-2-CYCLE-F-GATE-SUMMARY.md`,
`fleet/CONTROL.log` (seq 19–24 intact), heartbeats, `fleet/ORCH-STATE.md`, `fleet/queue/status.md`,
`fleet/queue/TASK-MAP.md`, and the queue files (`TASK-017/018/019/020/021.md` with the cut items and criteria).
Recovery followed the lane's documented pattern: `git fetch origin '+refs/heads/*:refs/remotes/origin/*'`, then
**`git reset --mixed origin/arena/01a0d9d0-fleetyard`** (never `--hard` — that would have destroyed the surviving
documents), which put HEAD back on the pushed head `31ce8dd` while keeping the working tree.

**Integrity checks after recovery.** `fleet2/activations/REGISTRY.md`: `git show origin/main:…` =
`a86115d2667e7d54…` and the working-tree copy = `a86115d2667e7d54…` → the **frozen registry is intact and was not
written by me** (nothing under `fleet2/` is staged or modified). `origin/main` is still **`7d033ab`** — no owner
errata landed while I was blind. The push is a clean fast-forward from `31ce8dd`.

**Disclosure the fleet is owed.** Anywhere the cycle-F documents name the shas `8cd2f5c` / `cba2550` / `3dd0b45` /
`1ca3941` / `430706b` / `4d297d0`, those were **local-only commits that no longer exist**; their content is what is
being pushed now, re-committed on top of `31ce8dd`. The pushed chain will therefore not contain those shas. The
**content** — verdicts, criteria, items cut, restrictions, digests — is authoritative and unchanged; only the
container commits were lost. Gate documents keep their original internal timestamps.

**Fleet state read on reconnect (I was blind from ~21:38Z to 2026-09-25T22:33:26Z).** BOSS-2 ran cycles 20–31, **zero controls, no
CONCERN against ORCH-2**, and recorded the reconnect at cycle 24 (21:52:50Z); the boss read my silence correctly as
"gating battery in progress". WORKER-2 moved `ffb8811` → **`4fc40c8`**: `d7fee6e` delivers **TASK-020 items 9–11**
(q4 holdout LAW §8 supplement, PATTERNS §5d exposure-normalized correction, the three stale ledger-digest lines +
q5's numbers; boss reports **suite 227 OK**), and `4fc40c8` binds the leg-(d) outcome into PATTERNS with the
**floor-5 sensitivity row 71/57/33/22 attached to every quotation of 57** — the citation rule from my beacon seq 18,
adopted independently. Neither commit could have seen my four verdicts, which were still unpushed: **TASK-018 items
0d–0g, TASK-019 v2.a/v2.b, TASK-020 items 8a/8b + criterion 20.13, TASK-017 item 17.a and TASK-021 are only now
becoming visible to the worker.**
